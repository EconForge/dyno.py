# Copilot Instructions

For this repository, follow AGENTS.md at the repository root.

Key rules:

- Use Pixi (`pixi run -e dev ...`) for all routine commands.
- Avoid direct pip/poetry workflows in this repo.
- Keep edits minimal and scoped to the request.
- Validate with tests and type checks before finalizing.
